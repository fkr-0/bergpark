{
  "name": "bergpark-wilhelmshoehe-guide",
  "version": "0.1.0-dev",
  "private": true,
  "type": "module",
  "description": "Offline-first bilingual PWA visitor guide for Bergpark Wilhelmshöhe",
  "scripts": {
    "predev": "node scripts/copy-data.mjs",
    "dev": "vite",
    "prebuild": "node scripts/copy-data.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "test:node": "node --test tests/*.test.mjs",
    "test:vitest": "vitest run",
    "test": "pnpm run test:node && pnpm run test:vitest",
    "test:py": "python -m unittest discover -s tests -p \"test_*.py\"",
    "test:e2e": "playwright test",
    "lint": "biome check --error-on-warnings src scripts tests vitest.config.mjs",
    "check:runtime-artifact": "node scripts/check-runtime-artifact.mjs",
    "check": "pnpm run lint && pnpm run test && pnpm run test:py && pnpm run build && pnpm run check:runtime-artifact",
    "check:e2e": "pnpm run check && pnpm run test:e2e"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "maplibre-gl": "6.6.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@biomejs/biome": "^2.5.10",
    "@playwright/test": "^1.62.1",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  },
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=22"
  }
}
